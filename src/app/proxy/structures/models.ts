export interface CreateUpdateStructureDto {
    id: string;
    libelle: string;
    // description: string;
    // active: boolean;
     code?:string;
}

export interface StructureDto {
    id?: string;
    libelle?: string;
    // description?: string;
    // active?: boolean;
     code?:string;
}

export interface CreateUpdateThemeDto {
    id: string;
    libelle?: string;
    description: string;
    // active: boolean;
}

export interface ThemeDto {
    id?: string;
    libelle?: string;
    description?: string;
    // active?: boolean;
}
